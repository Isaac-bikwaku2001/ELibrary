@model IEnumerable<ELibrary.Models.Exemplaire>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Livre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreExempalire)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Livre.Titre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreExempalire)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ExemplaireID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ExemplaireID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ExemplaireID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
